const firebaseClient = "TODO" // TODO: initialize firebase client

const getConfigData = (device_id) => {
    // TODO: implement function to get data from firebase client

    /*
        input: {
            device_id: ID of user device
        }
        output: {
            configData: structure to be decided (TODO)
        }
    */

    /*
        steps:
        1. Get data from firebaseClient for given row

        == THIS FUNCTION MIGHT NOT BE REQUIRED AS firebase MIGHT BE PROVIDING A GET METHOD ==
    */
}

export const configStore = {
    get: getConfigData
}